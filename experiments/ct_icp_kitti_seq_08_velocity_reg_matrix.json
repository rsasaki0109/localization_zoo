{
    "schema_version": 1,
    "problem": {
        "id": "ct_icp_kitti_seq_08_velocity_reg",
        "title": "CT-ICP seq 08 full: constant-velocity regularization (small weight)",
        "question": "Small velocity weights helped the long seq 00 (0.01). Does the same lever transfer to the other long sequence, seq 08 (4071 frames, prior best 1.945%)?",
        "dataset": {
            "pcd_dir": "dogfooding_results/kitti_seq_08_full",
            "gt_csv": "experiments/reference_data/kitti_seq_08_full_gt.csv"
        },
        "same_metrics": [
            "ate_m",
            "fps",
            "rpe_trans_pct",
            "readability_score",
            "extensibility_score"
        ]
    },
    "stable_interface": {
        "binary": "build/evaluation/pcd_dogfooding",
        "methods": "ct_icp",
        "primary_method": "CT-ICP"
    },
    "variants": [
        {
            "id": "corr_dist_reference",
            "label": "dense + corr-dist 4.0 + kp 0.5 (existing best, RPE 1.945%)",
            "design_style": "reference",
            "intent": "Confirm the prior seq 08 leaderboard-best recipe.",
            "args": [
                "--ct-icp-dense-profile",
                "--ct-icp-ceres-max-iterations", "8",
                "--ct-icp-max-frames-in-map", "30",
                "--ct-icp-max-correspondence-distance", "4.0",
                "--ct-icp-keypoint-voxel-size", "0.5"
            ]
        },
        {
            "id": "velocity_reg_001",
            "label": "+ constant-velocity-weight 0.01 (RPE 1.935%)",
            "design_style": "regularization",
            "intent": "Small velocity smoothing; brackets the optimum below 0.015.",
            "args": [
                "--ct-icp-dense-profile",
                "--ct-icp-ceres-max-iterations", "8",
                "--ct-icp-max-frames-in-map", "30",
                "--ct-icp-max-correspondence-distance", "4.0",
                "--ct-icp-keypoint-voxel-size", "0.5",
                "--ct-icp-constant-velocity-weight", "0.01"
            ]
        },
        {
            "id": "velocity_reg_0015",
            "label": "+ constant-velocity-weight 0.015 (winner, RPE 1.905%)",
            "design_style": "regularization",
            "intent": "Best seq 08 operating point: RPE -2.0% and ATE -24% (both improve).",
            "args": [
                "--ct-icp-dense-profile",
                "--ct-icp-ceres-max-iterations", "8",
                "--ct-icp-max-frames-in-map", "30",
                "--ct-icp-max-correspondence-distance", "4.0",
                "--ct-icp-keypoint-voxel-size", "0.5",
                "--ct-icp-constant-velocity-weight", "0.015"
            ]
        }
    ]
}
