{
  "schema_version": 1,
  "problem": {
    "id": "ct_icp_tuned_kitti_seq_00_full",
    "title": "CT-ICP tuned (dense + ceres_iter 6 + max_frames 20) on KITTI Odometry 00 (full)",
    "question": "Does the KITTI 07 winning tune close the gap on sequence 00?",
    "dataset": {
      "pcd_dir": "dogfooding_results/kitti_seq_00_full",
      "gt_csv": "experiments/reference_data/kitti_seq_00_full_gt.csv"
    },
    "same_metrics": ["ate_m", "fps", "rpe_trans_pct", "readability_score", "extensibility_score"]
  },
  "stable_interface": {
    "binary": "build/evaluation/pcd_dogfooding",
    "methods": "ct_icp",
    "primary_method": "CT-ICP"
  },
  "variants": [
    {
      "id": "tuned_dense_plus_budget",
      "label": "Tuned: dense + ceres_iter 6 + max_frames 20",
      "design_style": "tuned for paper parity",
      "intent": "Apply KITTI 07 gap-close winner across the paper-evaluated set.",
      "args": ["--ct-icp-dense-profile", "--ct-icp-ceres-max-iterations", "6", "--ct-icp-max-frames-in-map", "20"]
    }
  ]
}
